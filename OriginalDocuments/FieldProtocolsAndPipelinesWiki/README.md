# Field Protocols and Pipelines Wiki — Imported Originals

Verbatim copies of the markdown pages from the
[`APPN-Field-Protocols-and-Pipelines.wiki`](https://github.com/aus-plant-phenomics-network/APPN-Field-Protocols-and-Pipelines/wiki)
repository, imported on **2026-04-29**.

These files are kept here unchanged as a reference for their original HTML
formatting (e.g. `<details>` collapsibles, HTML tables) so the structure can be
restored later if desired. The canonical, GFM-converted versions live under
[`Protocols/`](../../Protocols/) and are published to the wiki by
[`Scripts/publish_to_wiki.py`](../../Scripts/publish_to_wiki.py) according to
[`publish.yaml`](../../publish.yaml).

Do not edit these files; treat them as a read-only archive.
